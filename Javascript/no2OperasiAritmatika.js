const operasiAritmatika = {
  ditambah : function(x, y) {
    return x + y;
  },

  dikurangin : function(x, y) {
    return x - y;
  },

  dikali : function(x, y) {
    return x * y;
  },

  dibagi : function(x, y) {
    return x / y;
  }
};

module.exports = operasiAritmatika;